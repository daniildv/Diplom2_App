<Window x:Class="Diplom2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diplom2"
        mc:Ignorable="d"
        Title="Окно Авторизации" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя плашка -->
        <Border Background="#2C3E50" Padding="30,25">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Личный кабинет УК «Наш Дом»"
                           Foreground="White" FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Вход для жителей"
                           Foreground="#B0C4DE" FontSize="14"
                           HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Форма -->
        <StackPanel Grid.Row="1" Margin="30,40,30,0" VerticalAlignment="Top">
            <TextBlock Text="Номер лицевого счёта" Foreground="#606266" FontSize="13" Margin="0,0,0,5"/>
            <TextBox x:Name="LoginTextBox" Margin="0,0,0,15" Height="35"/>

            <TextBlock Text="Пароль" Foreground="#606266" FontSize="13" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20" Height="35"/>

            <Button Content="Войти" Height="40" Background="#3498DB" Foreground="White"
                    FontWeight="SemiBold" FontSize="14" BorderThickness="0"
                    Margin="0,0,0,15" Cursor="Hand" Click="Button_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            
        </StackPanel>
    </Grid>
</Window>
